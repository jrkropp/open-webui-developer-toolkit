"""
Developer-facing CLI entrypoints for openai_responses_manifold.

Commands live in sibling modules (build, test, lint) and are registered in
`__main__.py` for use via `python -m openai_responses_manifold.cli` or the
console script declared in pyproject.toml.
"""

